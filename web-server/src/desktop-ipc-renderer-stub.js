function invoke() {
  return Promise.resolve(undefined)
}

function send() {}

function sendSync() {}

function on() {}

function once() {}

function removeListener() {}

module.exports = { invoke, send, sendSync, on, once, removeListener }
